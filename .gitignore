# onboarding state (contains Notion database ids — no secrets, but it's per-instance)
.ironbank_onboarding_state.json

# never commit credentials of any shape
config*.json
*.env
.env*
*secret*

# local scratch
*.log
__pycache__/

# personal financial data — never commit (names, amounts, backfill working files)
ManiTracker.xlsx
backfill_plan.csv
backfill_expenses_staged.csv

# local Claude Code tooling
.claude/
